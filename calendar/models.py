"""
Calendar app models. Placeholder until Calendar Entry models are defined per requirement doc.
"""
from django.db import models

# Models will be added when implementing Calendar Module (time entries, etc.)
